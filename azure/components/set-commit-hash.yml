steps:
  - bash: |
      echo "##vso[task.setvariable variable=COMMIT_HASH]$(echo "$BUILD_SOURCEVERSIONMESSAGE" | cut -d' ' -f2)"
    env:
      BUILD_SOURCEVERSIONMESSAGE: $(Build.SourceVersionMessage)
    displayName: Set COMMIT_HASH
